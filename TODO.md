# TODO - Analytics Dashboard

- [x] Step 1: Create `analytics_data.php` JSON endpoint to return time-series (daily/weekly/monthly/yearly) + product ranking + business status from `sales` and `sale_items`.
- [x] Step 2: Create `analytics.php` page with UI controls and Chart.js charts; fetch from `analytics_data.php`.
- [ ] Step 3: Add minimal CSS updates to match existing `dashboard.css` theme (charts/table layout).
- [ ] Step 4: Validate SQL assumptions (table/column names) and run a quick smoke test by loading `analytics.php` after seeding demo data.




